window.addEventListener("load", init);

function init() {
    let galleryButton = document.querySelector(".menu2");
    let settingsButton = document.querySelector(".menu3");

    galleryButton.addEventListener("click", showOverview)
}